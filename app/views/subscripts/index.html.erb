<h1>Compradores dos cursos</h1>
<h2>Perceba que muitos estão mudando suas vidas com ensino de excelencia!</h2>
<table id="subscripts">
  <tr>
    <th>#</th>
    <th>Nome</th>
    <th>Produto</th>
    <th>Escolaridade</th>
  </tr>
  <% @subscripts.each do |subscript| %>
    <tr>
      <td><%= subscript.id %></td>
      <td><%= subscript.name %></td>
      <td><%= subscript.language %></td>
      <td><%= subscript.schooling %></td>
    </tr>
  <% end %>
</table>